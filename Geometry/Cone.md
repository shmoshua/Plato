#Definition #Geometry 

> [!definition]
> For $V\subseteq \mathbb{R}^n$, 
> 1. the ***cone*** generated by $V$ is defined as:
> 	$$\text{cone}(V):=\left\{   \sum_{v\in V}^{}\lambda_{v}v: \lambda_{v}\geq 0  \right\}$$

---
##### Properties
> [!lemma] Theorem (Caratheodory)
> Let $v^1,\dots,v^k\in \mathbb{R}^n$. For $x\in \text{cone}(V)$,
> 1. there exists non-negative $\lambda$ s.t. $x=\sum_{i}^{}\lambda_{i}v^i$ and $\left| \text{supp}(\lambda) \right|\leq n$
---
##### Examples
> [!h] Example 1
> Let $A\in \mathbb{Z}^{m,n}$. Then,
> 1. $C:=\{ x\in \mathbb{R}^n: A_{1}x\leq 0,A_{2}x\geq 0 \}$ is a cone. 
> 2. $C=\text{cone}(u^1,\dots,u^t)$ for some $u^1,\dots,u^t\in \mathbb{Z}^n$ s.t. $\text{gcd}(u^i_{1},\dots,u^i_{n})=1$.
> 3. $\left\| u^i \right\|_{\infty}\leq \Delta:=\max \{ \left| \det B \right|:\text{ a square submatrix }B\text{ of }A \}$.
> 4.
 for every $b\in \mathbb{Z}^m$, if $x\in P$ and $x+C\subseteq P$, then for all 
> 